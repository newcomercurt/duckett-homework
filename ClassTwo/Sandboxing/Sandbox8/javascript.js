$('ul').on('click', function() {
  $('ul').find('#chirp').hide();
});

//the filter method will search through all of the element that are selected where
//the find method will search through the child elements of thaet selection a
